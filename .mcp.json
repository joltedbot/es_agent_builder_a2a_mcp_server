{
  "mcpServers": {
    "a2a-elastic": {
      "command": "node",
      "args": ["--env-file=.env", "dist/index.js"],
      "env": {
        "KIBANA_URL": "${KIBANA_URL}",
        "ELASTIC_API_KEY": "${ELASTIC_API_KEY}"
      }
    }
  }
}
